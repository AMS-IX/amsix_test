---
app: 'm6_provision'

# About interacting or not with myamsix (except final post,
# which now is a default)
use_myamisx_enabling    : 0 # also removes mac addresses if necessary
use_myamisx_testing     : 0 # does stuff with mac addresses, reactivvated for now
use_myamisx_mac_address : 0 # does all the mac addresses procedure

# Email notifications
mails_sender          : 'provision@ams-ix.net'
mails_noc             : 'noc@ams-ix.net'
customer_mails_sender : 'noc@ams-ix.net'
mails_cc              : [ 'gloria.garciadosil@ams-ix.net',
                          'laszlo.bogardi@ams-ix.net' ]

sudo            : '/usr/bin/sudo'
ndisc_header    : '/usr/bin/ndisc6'
ping_header     : $sudo /bin/ping

site_app: default_app
site_default: default_app
default_app: default_app
